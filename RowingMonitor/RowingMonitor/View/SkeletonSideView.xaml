<Page x:Class="RowingMonitor.View.SkeletonSideView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:RowingMonitor.View"
      xmlns:vm="clr-namespace:RowingMonitor.ViewModel"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="SkeletonSideView">

    <Page.DataContext>
        <vm:SkeletonSideViewModel x:Name="ViewModel"/>
    </Page.DataContext>

    <Grid x:Name="viewGrid">
        <Viewbox HorizontalAlignment="Center" MouseWheel="Viewbox_MouseWheel">
            <Image Source="{Binding SkeletonImageSource}" Stretch="UniformToFill"/>
        </Viewbox>
    </Grid>
</Page>
